# YÊU CẦU DỰ ÁN: TÍCH HỢP HỆ THỐNG E-COMMERCE, CỔNG THANH TOÁN MOMO, GIAO HÀNG NHANH (GHN) VÀ QUẢN TRỊ ĐƠN HÀNG
Bạn là một Chuyên gia Kỹ sư Fullstack Senior. Hãy triển khai hệ thống quản lý đơn hàng hoàn chỉnh tích hợp **Cổng thanh toán MoMo V2**, **Đơn vị vận chuyển Giao Hàng Nhanh (GHN Express API v2)**, **Luồng hủy đơn hoàn kho tự động** và **Trang quản trị Admin** theo đúng các quy chuẩn logic dưới đây:
---
### 1. CẤU TRÚC DATABASE (BẢNG ORDERS)
Bảng `orders` cần có đầy đủ các trường:
- `id`, `order_code` (Unique, dạng `CAM-XXXXXX`), `user_id` (nullable).
- `customer_name`, `customer_email`, `customer_phone`, `shipping_address`, `city`.
- `payment_method` (Enum/String: `'cod'`, `'vietqr'`, `'momo'`).
- `payment_status` (Enum/String: `'pending'`, `'completed'`, `'failed'`).
- `order_status` (Enum/String: `'pending'`, `'shipping'`, `'completed'`, `'cancelled'`).
- `total_amount` (Decimal), `shipping_fee` (Decimal), `discount_amount` (Decimal).
- `shipping_partner` (Default: `'GHN Express'`).
- `tracking_code` (String, lưu mã vận đơn trả về từ GHN).
- `ghn_order_code` (String), `expected_delivery_time` (String/Datetime).
- `cancel_reason` (Text, nullable).
---
### 2. LOGIC TÍCH HỢP CỔNG THANH TOÁN VÍ MOMO (MOMO V2 API)
Tạo `MomoService` với các quy tắc sau:
1. **Môi trường & Endpoint**:
   - Test: `https://test-payment.momo.vn/v2/gateway/api/create`
   - Biến môi trường: `MOMO_PARTNER_CODE`, `MOMO_ACCESS_KEY`, `MOMO_SECRET_KEY`, `MOMO_ENDPOINT`, `MOMO_REDIRECT_URL`, `MOMO_IPN_URL`.
2. **Thuật toán sinh chữ ký bảo mật HMAC-SHA256**:
   Chuỗi raw hash bắt buộc phải ghép theo đúng thứ tự Alphabet của key:
rawHash = "accessKey=" + accessKey + "&amount=" + amount + "&extraData=" + extraData + "&ipnUrl=" + ipnUrl + "&orderId=" + orderId + "&orderInfo=" + orderInfo + "&partnerCode=" + partnerCode + "&redirectUrl=" + redirectUrl + "&requestId=" + requestId + "&requestType=" + requestType; signature = hash_hmac("sha256", rawHash, secretKey);



3. **Xử lý Request & Webhook (IPN)**:
- Khi tạo giao dịch (`POST /payment/momo/create`), trả về `payUrl` để Frontend chuyển hướng khách hàng sang cổng MoMo quét mã.
- Endpoint `POST /payment/momo/ipn`: Kiểm tra chữ ký webhook với `verifyIpnSignature`. Khi `resultCode === 0`, tự động cập nhật `order->payment_status = 'completed'` và `order->order_status = 'shipping'`, đồng thời tự động gửi email hóa đơn cho khách.
- Hỗ trợ timeout 8s và cơ chế fallback mô phỏng sandbox nếu server MoMo test phản hồi chậm.
---
### 3. LOGIC TÍCH HỢP ĐƠN VỊ VẬN CHUYỂN GIAO HÀNG NHANH (GHN API V2)
Tạo `GHNService` với các quy tắc sau:
1. **Endpoint & Header**:
- URL: `https://online-gateway.ghn.vn/shiip/public-api/v2` (hoặc dev gateway nếu dùng test).
- Headers: `Token: {GHN_API_TOKEN}`, `ShopId: {GHN_SHOP_ID}`.
2. **Các chức năng cốt lõi**:
- Lấy danh mục hành chính: `/master-data/province`, `/master-data/district`, `/master-data/ward`.
- Tính phí ship thời gian thực (`/shipping-order/fee`): Gửi `from_district_id`, `to_district_id`, `to_ward_code`, `service_type_id = 2` (chuẩn E-commerce), `weight`.
- Đẩy đơn sang GHN (`/shipping-order/create`):
  - Gửi thông tin người nhận (`to_name`, `to_phone`, `to_address`, `to_ward_code`, `to_district_id`).
  - Tự động map `payment_type_id`: `1` (Người gửi trả cước nếu đơn đã thanh toán online) hoặc `2` (Người nhận trả cước nếu là đơn COD).
  - **Quy tắc an toàn hạn mức COD**: Nếu tài khoản shop chưa xác thực CCCD (bị giới hạn COD 60.000đ), tự động điều chỉnh `cod_amount = min(total_amount, 50000)` hoặc `0đ` để GHN không bao giờ trả lỗi `COD_IS_OVER_LIMIT`.
  - Nhận về `order_code` thật từ GHN (ví dụ: `GYYC6BP8`), lưu vào `orders.tracking_code` và chuyển trạng thái đơn sang `'shipping'`.
---
### 4. LOGIC HỦY ĐƠN HÀNG VÀ TỰ ĐỘNG HOÀN KHO (ORDER CANCELLATION & INVENTORY RESTOCK)
Endpoint `POST /orders/{id}/cancel`:
1. Chỉ cho phép hủy khi đơn hàng ở trạng thái `'pending'` (Chờ xử lý / đóng gói).
2. Khi hủy thành công:
- Duyệt qua từng sản phẩm trong `order_items`: `$product->increment('stock', $item->quantity)`.
- Cập nhật `order->order_status = 'cancelled'` và ghi nhận `cancel_reason`.
- Trả về thông báo thành công cho khách hàng và đồng bộ sang tab "Đã hủy".
---
### 5. LOGIC GIAO DIỆN & TRANG QUẢN TRỊ ADMIN (SPA PERSISTENCE & 1-CLICK DISPATCH)
1. **Trang Checkout (Khách hàng)**:
- Cung cấp 3 phương thức: `COD`, `VietQR`, `Ví MoMo`.
- Khi chọn MoMo và nhấn đặt hàng: Khởi tạo đơn hàng ➡️ Gọi API lấy `payUrl` ➡️ Chuyển hướng sang cổng thanh toán MoMo.
2. **Trang Lịch sử đơn hàng (`/orders`)**:
- Chỉ hiển thị đơn hàng thật từ database (không dùng mock/dummy data).
- Hiển thị badge phương thức (Ví MoMo màu hồng, VietQR màu xanh, COD).
- Hiển thị mã vận đơn thật của GHN kèm nút tra cứu hành trình.
3. **Trang Quản trị Admin (`/admin`)**:
- Quản lý danh sách đơn hàng thực tế kèm phân trang và lọc trạng thái.
- Thêm nút **`🚚 Đẩy GHN` (1-Click)** tại các đơn `'pending'`: Khi Admin bấm, hệ thống gọi GHN API tạo vận đơn thật và đổi trạng thái sang `'shipping'` ngay lập tức.
- **Đồng bộ URL & Phiên đăng nhập tức thì (0ms)**:
  - Khôi phục `user` đồng bộ từ `localStorage` để khi F5 trang Admin không bị giật hay nhảy về trang chủ.
  - Đồng bộ 2 chiều URL dạng `/admin?tab=orders`, `/admin?tab=products` bằng `window.history.pushState` & `replaceState`.
---
Hãy triển khai toàn bộ mã nguồn rõ ràng, có xử lý ngoại lệ (try-catch), ghi log (Log::error) và đảm bảo các file Controller, Service, Route và Component Frontend hoạt động tương thích 100% với nhau.# YÊU CẦU TRIỂN KHAI: TÍCH HỢP CỔNG THANH TOÁN VÍ MOMO (API V2 ALL-IN-ONE GATEWAY)
Bạn là một Chuyên gia Kỹ sư Backend & Fullstack. Nhiệm vụ của bạn là tích hợp cổng thanh toán trực tuyến MoMo vào dự án của tôi theo chuẩn MoMo Payment Gateway API v2.
Hệ thống phải hỗ trợ chế độ All-In-One (vừa có mã QR quét ứng dụng, vừa có form nhập số thẻ ATM nội địa Napas/NCB và Thẻ quốc tế Visa/Mastercard để người dùng và giảng viên test nhập liệu thực tế).
---
### 1. KIẾN TRÚC & QUY TRÌNH LUỒNG DỮ LIỆU (FLOW LOGIC)
1. **Khởi tạo thanh toán (Create Payment)**:
   - Khi khách hàng chọn phương thức "Thanh toán qua Ví MoMo" và nhấn Đặt hàng:
   - Hệ thống tạo đơn hàng trong bảng `orders` (trạng thái: `pending`).
   - Tạo một bản ghi giao dịch tài chính trong bảng `payment_transactions` (lưu vết: `order_id`, `gateway: 'momo'`, `gateway_order_id`, `amount`, `status: 'pending'`).
   - Gửi yêu cầu `POST` sang API MoMo Endpoint (`https://test-payment.momo.vn/v2/gateway/api/create`).
   - Nhận về `payUrl` và chuyển hướng trình duyệt của khách hàng trực tiếp sang cổng MoMo.
2. **Xử lý giới hạn số tiền MoMo Sandbox (Critical Safeguard)**:
   - MoMo Sandbox có quy định khắt khe: Số tiền giao dịch bắt buộc nằm trong khoảng **1.000 VNĐ đến 50.000.000 VNĐ**.
   - Nếu đơn hàng có giá trị > 50.000.000 VNĐ, MoMo sẽ báo lỗi `resultCode: 22`.
   - **Giải pháp**: Trong môi trường Test/Sandbox, nếu tổng tiền đơn hàng > 50.000.000 VNĐ, hệ thống tự động gán số tiền gửi sang MoMo là `50000000` (50 triệu) để API luôn chấp thuận và trả về link thanh toán thật.
3. **Cấu hình Request Type đa phương thức (All-In-One)**:
   - Tuyệt đối KHÔNG dùng `requestType = 'captureWallet'` (vì loại này chỉ hiện duy nhất mã QR, không có form nhập số thẻ ATM).
   - BẮT BUỘC dùng `requestType = 'payWithMethod'` kết hợp với tài khoản merchant test chuẩn để MoMo hiển thị đầy đủ các tab: Quét mã QR, Nhập thẻ ATM Napas, Nhập thẻ Visa/Mastercard.
4. **Xử lý kết quả thanh toán (2 kênh an toàn)**:
   - **Kênh 1: User Return Callback (GET `payment/momo/callback`)**: Trình duyệt của khách chuyển về sau khi bấm thanh toán thành công/hủy trên MoMo. Backend kiểm tra tính hợp lệ của chữ ký (signature) và `resultCode == 0`:
     + Cập nhật `payment_transactions.status = 'paid'`, lưu mã `transId`.
     + Cập nhật `orders.payment_status = 'completed'` và `orders.order_status = 'processing'` (hoặc `shipping`).
     + Chuyển hướng người dùng về trang Thông báo Đặt hàng thành công (`/order-success?id=...`).
   - **Kênh 2: Server-to-Server IPN Webhook (POST `api/v1/payment/momo/ipn`)**: Lắng nghe thông báo ngầm từ máy chủ MoMo đề phòng trường hợp khách hàng thanh toán xong nhưng vô tình tắt trình duyệt.
5. **Chức năng Thanh toán lại (Pay Again)**:
   - Khi một đơn hàng chưa thanh toán hoặc thanh toán thất bại, cho phép khách hàng bấm "Thanh toán lại MoMo" mà không cần tạo đơn hàng mới. Tạo bản ghi transaction mới và cấp link MoMo mới.
---
### 2. THIẾT KẾ CƠ SỞ DỮ LIỆU (DATABASE SCHEMA)
#### Bảng `payment_transactions`:
```sql
CREATE TABLE payment_transactions (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    order_id BIGINT UNSIGNED NOT NULL,
    gateway VARCHAR(50) NOT NULL DEFAULT 'momo',
    gateway_order_id VARCHAR(100) NULL,
    transaction_id VARCHAR(100) NULL, -- Mã giao dịch do MoMo trả về (transId)
    amount DECIMAL(15, 2) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'pending', -- pending, initiated, paid, failed
    result_code INT NULL,
    message VARCHAR(255) NULL,
    request_payload JSON NULL,
    response_payload JSON NULL,
    paid_at TIMESTAMP NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE
);
3. CẤU HÌNH THÔNG SỐ (.ENV) CHUẨN TEST LAB & TÀI LIỆU MOMO
env


# Cấu hình MoMo Sandbox All-In-One (Đầy đủ Form nhập thẻ ATM/Napas + Mã QR)
MOMO_PARTNER_CODE=MOMOBKUN20180529
MOMO_ACCESS_KEY=klm05TvNBzhg7h7j
MOMO_SECRET_KEY=at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa
MOMO_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/create
MOMO_REDIRECT_URL=http://127.0.0.1:8000/payment/momo/callback
MOMO_IPN_URL=http://127.0.0.1:8000/api/v1/payment/momo/ipn
MOMO_REQUEST_TYPE=payWithMethod
MOMO_VERIFY_SSL=false
4. LOGIC TẠO CHỮ KÝ HMAC-SHA256 (MOMOSERVICE)
Quy tắc tạo chữ ký MoMo API v2: Các tham số phải được sắp xếp theo đúng thứ tự alphabet như sau:

php


$rawHash = 'accessKey=' . $accessKey .
    '&amount=' . $amount .
    '&extraData=' . $extraData .
    '&ipnUrl=' . $ipnUrl .
    '&orderId=' . $gatewayOrderId .
    '&orderInfo=' . $orderInfo .
    '&partnerCode=' . $partnerCode .
    '&redirectUrl=' . $redirectUrl .
    '&requestId=' . $requestId .
    '&requestType=' . $requestType;
$signature = hash_hmac('sha256', $rawHash, $secretKey);
Kiểm tra chữ ký khi MoMo gửi callback về:

php


$rawHash = 'accessKey=' . $accessKey .
    '&amount=' . $data['amount'] .
    '&extraData=' . $data['extraData'] .
    '&message=' . $data['message'] .
    '&orderId=' . $data['orderId'] .
    '&orderInfo=' . $data['orderInfo'] .
    '&orderType=' . $data['orderType'] .
    '&partnerCode=' . $data['partnerCode'] .
    '&payType=' . $data['payType'] .
    '&requestId=' . $data['requestId'] .
    '&responseTime=' . $data['responseTime'] .
    '&resultCode=' . $data['resultCode'] .
    '&transId=' . $data['transId'];
$expectedSignature = hash_hmac('sha256', $rawHash, $secretKey);
return hash_equals($expectedSignature, $data['signature'] ?? '');
5. DỮ LIỆU DEMO DÀNH CHO TEST NHẬP THẺ TRÊN CỔNG MOMO
Cung cấp sẵn trên giao diện để người dùng/giảng viên nhập test:

Ngân hàng: NCB (hoặc bất kỳ ngân hàng nào trong danh sách hỗ trợ)
Số thẻ ATM: 9704198526191432198
Tên in trên thẻ: NGUYEN VAN A
Ngày phát hành: 07/15
Mã OTP xác thực: 000000
Hoặc dùng App MoMo Developer test với SĐT: 0968202605 (OTP: 000000).
6. NHIỆM VỤ CẦN BẠN THỰC HIỆN
Tạo migration tạo bảng payment_transactions nếu chưa có.
Viết class MomoService với đầy đủ các phương thức: createPayment(), isValidResponse(), isSuccessful(), markPaid(), markFailed().
Viết PaymentController xử lý khởi tạo thanh toán (createMomoPayment), thanh toán lại (payAgain), tiếp nhận phản hồi (handleMomoCallback) và webhook IPN (handleMomoIpn).
Tích hợp nút thanh toán MoMo và nút chuyển hướng trực tiếp sang MoMo trên Frontend.
Kiểm tra kỹ lưỡng, đảm bảo mã nguồn sạch sẽ, xử lý ngoại lệ đầy đủ và sẵn sàng chạy thử nghiệm.